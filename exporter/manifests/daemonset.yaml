apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: edge-metrics-exporter-jetson
  namespace: monitoring
  labels:
    app: edge-metrics-exporter-jetson
spec:
  selector:
    matchLabels:
      app: edge-metrics-exporter-jetson
  updateStrategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: edge-metrics-exporter-jetson
    spec:
      nodeSelector:
        device-family: jetson
      tolerations:
        - key: node-role.kubernetes.io/edge
          effect: NoSchedule
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      containers:
        - name: exporter
          image: edge-metrics-exporter:latest
          imagePullPolicy: IfNotPresent
          securityContext:
            privileged: true
          ports:
            - name: metrics
              containerPort: 9102
            - name: management
              containerPort: 9101
            - name: shelly-ws
              containerPort: 8765
            - name: shelly-http
              containerPort: 8766
          env:
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            - name: CONFIG_SERVER_URL
              value: "http://edge-metrics-server.monitoring.svc.cluster.local:8081"
            - name: CONFIG_TIMEOUT
              value: "5"
            - name: LOCAL_CONFIG_PATH
              value: "/config/config.yaml"
            - name: SHELLY_ENABLED
              value: "true"
          volumeMounts:
            - name: config-dir
              mountPath: /config
            - name: tegrastats-bin
              mountPath: /usr/bin/tegrastats
              readOnly: true
          livenessProbe:
            httpGet:
              path: /health
              port: 9101
            initialDelaySeconds: 15
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 9101
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "500m"
      volumes:
        - name: config-dir
          hostPath:
            path: /var/lib/edge-metrics
            type: DirectoryOrCreate
        - name: tegrastats-bin
          hostPath:
            path: /usr/bin/tegrastats
            type: File
